{
	"scripts": {
		"dev": "npm-run-all --parallel server",
		"server": "browser-sync start --server --watch"
	},
	"devDependencies": {
		"browser-sync": "^2.28.3",
		"npm-run-all": "^4.1.5"
	},
	"browserslist": [
		"defaults"
	],
	"dependencies": {
		"uuid": "^9.0.0"
	}
}